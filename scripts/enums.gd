extends Node

class_name E

## The names and values of this enum must match the audio bus configuration.
enum AudioBus {
	Master = 0,
	Music = 1,
	Sfx = 2,
	UI = 3,
}

enum Sound {
	Ui_Click,
}

enum Level {
	Level1,
	Level2,
	Level3,
	Level4,
	Level5,
	End,
}

enum Element {
	Water,
	Fire,
	Earth,
	Wind,
	All,
}

enum Enemy {
	Boss,
	EndBoss,
	Mini,
}

enum AttackMode {
	Fire,
	Water,
	Air,
	Earth,
}
